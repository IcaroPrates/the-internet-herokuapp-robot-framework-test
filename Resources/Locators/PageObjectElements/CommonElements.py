# coding=utf-8
# Global elements that are used in more than one web test

# PageObject file name must be the name of the page, using:
# UpperCamelCase for file name.
# snake_case for variable names.

page_title_header = "xpath://h3[contains(text(), '{PARAMETER}')]"
